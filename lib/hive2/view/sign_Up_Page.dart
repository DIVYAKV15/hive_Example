import 'package:flutter/material.dart';

class HiveSignUpPage extends StatelessWidget {
  const HiveSignUpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar: AppBar(title: Text("WELCOME"),),);
  }
}

